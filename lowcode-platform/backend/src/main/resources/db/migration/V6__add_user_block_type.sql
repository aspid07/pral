-- USER — человек, который в теории может запускать/останавливать сценарий
-- (в отличие от ACTOR, который в исходном MVP предполагался как внешняя
-- система/актор в терминологии use-case диаграмм — см. vision-and-scope.md).
-- block_type — открытый для расширения справочник (см. BlockType.java), новый
-- код добавляется просто строкой, никакого enum на Java-стороне менять не нужно.
insert into block_type (id, code, display_name) values
    (gen_random_uuid(), 'USER', 'Пользователь');
